plot(interest_rate ~ debt_to_income, 
     data = loans_sampled, col = "grey", pch = 20,
     main = "Data from Model 1")

plot(interest_rate ~ delinq_2y, 
     data = loans_sampled, col = "grey", pch = 20,
     main = "Data from Model 1")

plot(interest_rate ~ inquiries_last_12m, 
     data = loans_sampled, col = "grey", pch = 20,
     main = "Data from Model 1")

plot(interest_rate ~ total_credit_limit, 
     data = loans_sampled, col = "grey", pch = 20,
     main = "Data from Model 1")

plot(interest_rate ~ accounts_opened_24m, 
     data = loans_sampled, col = "grey", pch = 20,
     main = "Data from Model 1")

plot(interest_rate ~ num_active_debit_accounts, 
     data = loans_sampled, col = "grey", pch = 20,
     main = "Data from Model 1")

plot(interest_rate ~ total_debit_limit, 
     data = loans_sampled, col = "grey", pch = 20,
     main = "Data from Model 1")

plot(interest_rate ~ num_total_cc_accounts, 
     data = loans_sampled, col = "grey", pch = 20,
     main = "Data from Model 1")

plot(interest_rate ~ num_mort_accounts, 
     data = loans_sampled, col = "grey", pch = 20,
     main = "Data from Model 1")

plot(interest_rate ~ loan_amount, 
     data = loans_sampled, col = "grey", pch = 20,
     main = "Data from Model 1")

plot(interest_rate ~ balance, 
     data = loans_sampled, col = "grey", pch = 20,
     main = "Data from Model 1")

plot(interest_rate ~ paid_interest, 
     data = loans_sampled, col = "grey", pch = 20,
     main = "Data from Model 1")
